import styled from '@emotion/styled';

export const Paragraph = styled.p`
  text-align: justify;
  margin: auto;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  font-size: 0.8rem;
`;
